# 9tail asset pack

Drop the sprite files for the new pet here.

Expected filenames:
- `walk.gif` or `default_walk_8fps.gif`
- `idle.gif` or `default_idle_8fps.gif`
- Optional PNG frame sequences named `walk_*.png` and `idle_*.png`

The app will discover this folder automatically when GIF or PNG sprite files are present.
